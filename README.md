# Symfony 5 with Docker Boilerplate

## Dependencies

- Docker

## Run the project

- `docker-compose up -d`

## Instructions

- Run Symfony commands with `php` container. e.g: `docker-compose exec php php bin/console doctrine:schema:update --force`